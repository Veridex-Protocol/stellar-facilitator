# Dependencies
node_modules/
bun.lockb
yarn.lock

# package-lock.json is deliberately NOT ignored. `npm ci` reads it, and the
# reproducibility claim in README.md depends on a clean clone resolving the
# same dependency tree this repository was tested against.

# Build outputs
dist/
build/
*.wasm
*.optimized.wasm

# Environment files
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Test coverage
coverage/
.nyc_output/

# Temporary files
tmp/
temp/
*.tmp

# OS files
.DS_Store
Thumbs.db

# Rust
target/
**/*.rs.bk

# Cargo.lock is deliberately NOT ignored. CI runs `cargo test --locked`, and the
# wasm hash a facilitator verifies before advertising the upto scheme is only
# reproducible if the dependency graph is pinned.

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
.venv/
*.egg-info/
dist/
build/
*.egg

# Database
*.db
*.sqlite
*.sqlite3

# Secrets
*.pem
*.key
!*.key.example

# Competitive and ecosystem survey material. Third-party repositories cloned
# for analysis live here and must never be committed into this one: they carry
# their own licences and history, and vendoring them would misrepresent whose
# work this repository contains.
research/

# Internal working notes, not part of the published repository.
notes
push_all.sh
GITHUB_SETUP.md
IMPLEMENTATION_PLAN.md
PROGRESS.md
BATCH2_COMPLETE.md
BATCH3_COMPLETE.md
SCF_BUILD_INTEREST_FORM_SUBMISSION.md

# Conformance output: regenerated by every run.
conformance-report.json
# docs/adr/
audit.html
