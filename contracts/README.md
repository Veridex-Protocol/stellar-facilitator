# Stellar `upto` contracts

`upto-settlement` is the v2 reference contract and the only contract intended for new deployments.

`upto_escrow` is an earlier prepaid-escrow experiment retained for migration reference. It is not the x402 v2 `upto` implementation, is not advertised by the facilitator, and must not be deployed as the v2 contract.
