# This directory is a standalone package that happens to live inside a
# monorepo whose root declares `workspaces`. Without this, `npm install`
# walks up, finds that root, and tries to reify the entire monorepo.
workspaces=false
